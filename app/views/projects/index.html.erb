<h1>Projects</h1>

<p style="color: green"><%= notice %></p>

<!-- create a for loop here to iterate over the number of projects -->
<!-- be sure to setup logic for limiting number of projects per row -->

<div id="projects">
  <% @projects.each do |project| %>
    <%= render project %>
    <p>
      <%= link_to "Show this project", project %>
    </p>
  <% end %>
</div>

<%= link_to "New project", new_project_path %>
