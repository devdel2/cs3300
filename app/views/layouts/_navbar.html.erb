<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= projects_path %>">Dev&#39s Portfolio</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" aria-current="page" href="<%= projects_path %>">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="https://github.com/devdel2">Github</a>
            </li>
        </ul>
        <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
                <a class="nav-link" aria-current="page" href="<%= destroy_user_session_path %>">Logout</a>
            <% else %>
                <a class="nav-link" aria-current="page" href="<%= new_user_session_path %>">Login</a>
                <a class="nav-link" href="<%= new_user_registration_path %>">Sign-Up</a>
            <% end %>
        </ul>
    </div>
  </div>
</nav>